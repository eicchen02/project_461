# This is to remove HTML and CSS files from being calculated into percentage.
# HTML and CSS files were generated from an online program, in order to create
# a webpage, so we do not believe they should be factored into the percentage
# calculation.
*.html -linguist-detectable
